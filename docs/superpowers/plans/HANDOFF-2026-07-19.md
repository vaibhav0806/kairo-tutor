# Auth + Billing + Onboarding — Build Handoff (2026-07-19)

Autonomous build session summary. **The backend (auth + metering + billing) is complete,
tested, and live-verified.** The desktop cutover and onboarding UI remain (detailed below).

---

## TL;DR

| Subsystem | Status |
|---|---|
| **Monorepo scaffold** (npm workspaces, `@kairo/shared`, agent-rules split) | ✅ done |
| **Plan 1 — Backend + Auth spine** (`server/`) | ✅ done, 12 tests green, live-verified vs Neon + Google |
| **Plan 3 — Billing** (Dodo entitlement engine, checkout, webhook) | ✅ code done, 3 tests green, checkout live-verified (test mode) |
| **Native auth** (deep-link + Keychain + auth commands) | ✅ done, cargo check clean |
| **Onboarding UI** (AI-assisted, voice-or-type) | ✅ done, typecheck + vite build green (needs live UI verification) |
| **Plan 2b — Provider repoint** (route the 12 calls through the proxy) | ⏳ foundation in (`USE_BACKEND_PROXY` flag + `fetch_jwt`); per-site recipe below |

The desktop **tutor/voice flow is unchanged** (still calls providers directly — `USE_BACKEND_PROXY`
defaults off). New this session: native Google auth (additive) and the onboarding flow (shown only
on first run). Nothing in the working path is broken.

### Session 2 additions (done)
- **Native auth** (`src-tauri/src/auth.rs`): `tauri-plugin-deep-link` (kairo:// scheme) + `keyring`
  (macOS Keychain). Commands `start_google_auth`, `get_auth_status`, `get_backend_jwt`, `sign_out`;
  the `kairo://auth-callback` handler exchanges the one-time code → session token in Keychain →
  emits `auth:changed` (no activation-policy change). cargo check clean.
- **Onboarding UI** (`src/onboarding/*`, gated by `src/AppRoot.tsx`): welcome → name → Google
  sign-in → where-did-you-find-us → permissions → teach (hold ⌥⌃ to talk; "I point, you act") →
  done. Kairo speaks each step (Sarvam TTS via the unauth endpoint), answer by voice or type, live
  violet waveform, dark-glass violet UI, compact centered window. Typecheck + vite build green.
  **Needs live UI verification** (I can't render it here) + one Google sign-in click-through.

### Plan 2b — provider repoint recipe (mechanical, do with the backend running + a real ask)
For each site, when `constants::USE_BACKEND_PROXY` is true: fetch a JWT (`auth::fetch_jwt().await`),
point at the semantic backend route (NOT the provider path), and send `Authorization: Bearer <jwt>`
instead of the provider key. Bodies forward verbatim except the tutor turn. Sites (from the grep):

| Rust site | today | → backend route | notes |
|---|---|---|---|
| `tutor.rs` `run_gate_turn`/`run_ack_turn` (`openrouter_text_chat`, ~534) | `{base}/chat/completions` | `{KAIRO_BACKEND_URL}/v1/llm/chat` | body unchanged |
| `grounding.rs` `openai_vision_chat` (~198) | `{base}/v1/responses` | `/v1/vision/tutor` | add `"_provider":"openai"` to body + header `X-Kairo-Ask-Id` |
| `grounding.rs` `anthropic_vision_chat` (~112) | `{base}/v1/messages` | `/v1/vision/tutor` | add `"_provider":"anthropic"` + `X-Kairo-Ask-Id` |
| `grounding.rs` `detect_click_point_openai` (~642) | `{base}/v1/responses` | `/v1/vision/point` | body unchanged |
| `speech.rs` `transcribe_audio` (~196) | `{base}/speech-to-text` | `/v1/stt` | multipart unchanged |
| `speech.rs` `synthesize_speech` (~305) | `{base}/text-to-speech` | `/v1/tts` | body unchanged |
| `speech.rs` `synthesize_speech_stream` (~392) | `/text-to-speech/stream` | `/v1/tts/stream` | chunk loop unchanged |

Mint one `ask_id` (uuid) per user ask in the frontend/notch and thread it to the tutor turn only
(that's the metered call). Verify: `npm run server:dev`, flip `USE_BACKEND_PROXY=true`, rebuild,
sign in, ask — watch `used_free` increment and a 402 at the limit.

15 tests pass (`npm run test -w @kairo/server`). Typecheck clean. Prod bundle builds (`tsup`).

---

## What's done + verified

### Backend (`server/`) — Fastify + Neon (Drizzle) + Better Auth + AI proxy
- **Auth:** self-hosted Better Auth 1.6.23, Google-only, JWT (15m) + JWKS + bearer. Three owned
  routes for the desktop `kairo://` handshake: `GET /auth/start` → Google, `GET /auth/callback` →
  `kairo://auth-callback?code=…`, `POST /auth/exchange {code}` → `{ sessionToken, expiresAt }`.
  `POST /api/auth/token` (bearer=sessionToken) → JWT; `GET /api/auth/jwks`.
  - **Live-verified:** `/auth/start` returns a real 302 to `accounts.google.com` with PKCE (S256),
    scopes `email profile openid`, redirect `http://localhost:8787/api/auth/callback/google`.
  - `/auth/exchange` creates a session row directly (1.6 has no server `createSession`); proven to
    authenticate `/api/auth/token` and yield a JWT.
- **Metering:** `usage_counter` (used_free / free_limit / plan), atomic
  `UPDATE … WHERE used_free < free_limit RETURNING` reserve + idempotent `usage_event` + refund on
  failure. Metered on **`POST /v1/vision/tutor` only** (= 10 free asks). Verified 200→402.
- **AI proxy (key-injecting, JWT-gated):** `POST /v1/llm/chat` (OpenRouter), `POST /v1/vision/tutor`
  (metered; `_provider:'anthropic'|'openai'` picks the upstream), `POST /v1/vision/point` (OpenAI),
  `POST /v1/stt` (multipart→Sarvam), `POST /v1/tts`, `POST /v1/tts/stream` (PCM passthrough, aborts
  upstream on client disconnect). `GET /v1/me` → `{plan,status,usage,renews_at,paywalled}`.
- **Billing (Dodo):** `applyDodoState()` maps the subscription lifecycle → `subscription` row +
  denormalized `usage_counter.plan` (cancel keeps pro to period end, on_hold 3-day grace, expire→
  free), out-of-order guard, webhook idempotency (`webhook_event`). `POST /webhooks/dodo` (scoped
  raw-body + Standard Webhooks HMAC). `POST /v1/billing/checkout` + `/v1/billing/portal`.
  - **Live-verified (test mode):** created the two Pro products and a real checkout session.

### Test artifacts (Dodo **test mode**, saved in `server/.env`)
- `DODO_PRO_MONTHLY_PRODUCT_ID=pdt_0NjWYTUCVziZfBIvDHzHM`
- `DODO_PRO_YEARLY_PRODUCT_ID=pdt_0NjWYTVoEd4ebNMfRFn0z`

### Run / test the backend
```bash
npm run db:migrate -w @kairo/server      # applies migrations to Neon
npm run server:dev                        # dev server on :8787
npm run test -w @kairo/server             # 15 tests (against the Neon dev branch)
npm run build -w @kairo/server            # tsup -> self-contained dist/
```

---

## Remaining — Plan 2: Desktop cutover (Rust + frontend)

Goal: the app authenticates via the browser and routes **all** provider calls through the backend.
**Important:** the backend uses **semantic** routes (not a transparent base-URL passthrough), so
each Rust call site maps to a specific route + sends the same provider body, swapping the provider
key for the session JWT.

**Recommendation:** gate the provider-repoint behind a `USE_BACKEND_PROXY` constant (default
`false`) so the working direct-provider path stays intact until you flip it with the backend running.

### Native (Rust, `src-tauri/`)
1. `Cargo.toml`: add `tauri-plugin-deep-link` (v2) and `keyring` (v3).
2. `tauri.conf.json`: `plugins.deep-link.desktop.schemes = ["kairo"]`. Verify it injects
   `CFBundleURLTypes` into `Info.plist` after a build (add manually if not). **No entitlement needed.**
3. New `src-tauri/src/auth.rs`:
   - Keychain via `keyring` (service `com.kairo.tutor`, items `session` / `refresh`), `get/set/clear`.
   - `start_google_auth()` command → open the system browser at `{KAIRO_BACKEND_URL}/auth/start`
     (reuse the `open` pattern in `permissions.rs`).
   - deep-link handler (registered in `lib.rs` `setup()` via `on_open_url`): parse
     `kairo://auth-callback?code=…` → `POST {backend}/auth/exchange {code}` → store `sessionToken`
     in Keychain → `app.emit("auth:changed", …)`. **Must not** foreground the app (keep Accessory
     policy — don't call `set_activation_policy(Regular)`). **Redact** the code in logs.
   - Token helper: cache a JWT from `POST {backend}/api/auth/token` (bearer=sessionToken), refetch
     on 401. `get_auth_status()` and `sign_out()` commands.
4. `constants.rs`: add `KAIRO_BACKEND_URL` (e.g. `http://localhost:8787`) + `USE_BACKEND_PROXY:bool`.
5. Repoint the 12 provider call sites (behind the flag). Map each to the semantic route, attach
   `Authorization: Bearer <jwt>`, drop the provider key:
   - `run_gate_turn` / `run_ack_turn` (OpenRouter chat) → `POST /v1/llm/chat` (same chat body).
   - `run_tutor_turn` vision → `POST /v1/vision/tutor` with the existing OpenAI-Responses/Anthropic
     body **plus** `_provider` and header `X-Kairo-Ask-Id: <uuid minted per ask>`.
   - `detect_click_point_openai` → `POST /v1/vision/point`.
   - `transcribe_audio` → `POST /v1/stt` (multipart, one `file` field).
   - `synthesize_speech` → `POST /v1/tts`; `synthesize_speech_stream` → `POST /v1/tts/stream`
     (unchanged chunk loop; only URL + bearer change).
   - Centralize in one `backend_request(path)` helper next to `shared_http_client`.
6. Typed errors: map backend `401`→`unauthenticated`, `402`→`quota_exceeded`, network→`offline`, so
   the notch can branch (see frontend below). `cargo check` green before committing.

### Frontend (`src/`)
7. `src/core/auth.ts`: `startGoogleAuth() / getAuthStatus() / signOut()` bridge wrappers + a
   `useAuth()` hook that reads initial state and subscribes to the `auth:changed` Tauri event.
8. `src/native/nativeBridge.ts`: add the auth methods (with browser fallbacks).
9. `src/core/tutorErrors.ts` + `src/core/runtimePlanner.ts`: add a typed `quota_exceeded` /
   `auth_required` branch instead of the generic "AI unavailable".
10. `src/App.tsx`: a "Sign in with Google" affordance gated on `useAuth()` (fuller flow in Plan 4).

---

## Remaining — Plan 4: Onboarding UI (frontend)

3-step first-run flow in the **main window** (`App.tsx`), internal step state (not a new WebView):
1. **Sign in with Google** (`useAuth`).
2. **Grant permissions** — reuse the existing `App.tsx` permission UI (FEATURE.md §6.2) verbatim.
3. **Ready** — show the shortcuts.
Plus an account area (plan / "N free left" / Manage → `/v1/billing/portal`) and the hard-paywall
reaction in the notch (on `quota_exceeded`: "Out of free tutors — open Kairo to upgrade" → focus
main window → open the Dodo checkout URL from `/v1/billing/checkout`; return via `kairo://billing-done`).
Refactor targets while here (behavior-preserving): split `notch/NotchApp.tsx` (2498 lines) into a
thin view + hooks; extract `tutor/`, `speech/`, `gesture/` out of `notch/`; fix `server/providers/`
naming (see design doc §8.2).

---

## Remaining — Billing finish (needs your Dodo account)

- Complete Dodo **KYC / business verification** (for live payouts) — dashboard only.
- Create the webhook endpoint (`{backend}/webhooks/dodo`) in the Dodo dashboard and put its signing
  secret in `DODO_PAYMENTS_WEBHOOK_SECRET` (server env). Then the lifecycle sync is fully live.
  (The webhook receiver + HMAC verify are coded; only a real delivery is untested.)
- For **live** mode later: create live products, put the live key + live product IDs only on Hetzner.

---

## Notes / decisions taken during the build

- **Semantic routes, not transparent passthrough** — cleaner metering + key handling. Desktop cutover
  maps per-route (above).
- **`@kairo/shared` is source-only**; the server bundles it via `tsup` (`noExternal`) so prod
  `node dist` is self-contained.
- **Sessions in `/auth/exchange`** are created by direct row insert (no server `createSession` in
  1.6). Verified to bearer-authenticate.
- **Tests run sequentially** (`fileParallelism:false`) — they share one Neon DB and some listen on
  8787 for the JWKS fetch.
- **Neon SSL:** `rejectUnauthorized:false` (common Neon dev setting); tighten for prod if desired.
  A pg v9 deprecation warning about `sslmode` aliases is harmless for now.
- **Security:** no secrets committed; `server/.env` gitignored (verified). Dodo operated in **test
  mode only**. Live Dodo key still sits in root `.env` — remove it when convenient (per your note).
